import styled from '@emotion/styled';

const Input = styled.input`
  margin-top: 10px;
  width: 200px;
  height: 30px;

  font-size: 24px;

  & ~ p {
    font-size: 14px;
  }
`;

export default Input;
